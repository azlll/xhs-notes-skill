---
name: xhs-notes-skill
description: 帮助创作者高效策划、生成和复盘小红书/XHS/RedNote 图文笔记。适用于用户要做笔记、爆款标题、图文方案、图片生成 Prompt、正文、标签、评论钩子、图片内容分析、发布前风险检查、发布后数据复盘或生成本地 HTML 可视化复盘看板时。支持视觉理解模式、文字规划模式和发布后复盘模式。
license: MIT
compatibility: 适用于支持读取本地文件和图片的 Agent；直接生图需要 Python 3.9+、网络访问和 OpenAI-compatible Images API 配置。
---

# 小红书图文笔记创作 Skill

## 使用原则

- 先判断输入模式：用户提供图片时优先使用视觉理解模式；用户提供主题、关键词或想法时使用文字规划模式。
- 每个新的图文笔记任务首次触发时，先按 [动态澄清规则](references/dynamic_clarification.md) 和 [主题确认与选题规则](references/topic_selection.md) 执行强制确认主题；未确认主题不得生成最终笔记、标题、正文或标签。
- 提问时优先使用客户端可用的结构化选项能力（如 `request_user_input`、选项框、chips）；不可用时改用简短文本选项，不要声称已弹窗。
- 不承诺“必爆”“稳赚”“根治”等绝对结果，可使用“提升点击率”“更容易被收藏”“更有讨论感”等审慎表达。
- 不编造图片中不存在的品牌、地点、价格、功效、身份或经历；不确定的信息标注为“可选设定”或向用户确认。
- 默认输出中文，语气适合小红书：具体、轻盈、有画面感，但避免过度营销。
- 每次生成标题前联网搜索：基于已确认主题，按 [公式联网刷新规则](references/live_formula_refresh.md) 做主题强相关联网搜索，覆盖实时新闻、节点、热梗、平台表达和热门标签；失败或无可靠结果时回退内置公式库。
- 最终输出前必须执行风险检查，必要时读取 [风险清单](references/risk_checklist.md)。
- 用户要求“复盘这篇”“分析数据”“看这个笔记/主页链接表现”“生成复盘报告/看板/HTML”时，进入发布后复盘模式，先读取 [发布后复盘规则](references/post_publish_review.md)；复盘任务不强制执行新笔记主题确认。
- 若用户明确要求直接生图，先检查配置；缺少配置时提示用户配置，不得假装已生成图片。

## 按需加载资料

- 新任务首次触发或用户意图不清时，读取 [动态澄清规则](references/dynamic_clarification.md)。
- 新任务需要确认主题或生成选题建议时，读取 [主题确认与选题规则](references/topic_selection.md)。
- 使用标题、正文、标签公式前，读取 [公式联网刷新规则](references/live_formula_refresh.md)。
- 需要标题时，读取 [标题公式库](references/title_formulas.md)。
- 需要正文结构时，读取 [正文模板](references/content_templates.md)。
- 需要标签时，读取 [标签策略](references/tags_strategy.md)。
- 需要评论互动设计时，读取 [评论钩子库](references/hooks_library.md)。
- 需要加入热梗热词、平台流行表达或年轻化语气时，读取 [热词趋势库](references/trend_keywords.md) 和 [热梗句式库](references/meme_sentence_patterns.md)。
- 用户要求“最新/近期/今日/本周”热梗热词时，先按 [热梗热词更新规则](references/trend_update_rules.md) 校验时效，再决定是否使用。
- 用户提供图片时，先读取 [图片理解提示词](references/image_understanding_prompt.md)。
- 需要合规、违禁词替换或 AI 标注时，读取 [风险清单](references/risk_checklist.md)。
- 调用生图脚本或 HTML 报告脚本前，先按 [脚本降级输出规则](references/fallback_outputs.md) 判断脚本、Python 环境和输出目录是否可用；不可用时改用纯文本降级输出。
- 需要复盘已发布笔记、分析数据、处理笔记链接/主页链接或生成 HTML 看板时，读取 [发布后复盘规则](references/post_publish_review.md)。
- 需要图片生成 Prompt 时，读取 [构图技巧](references/prompts/composition.md)、[风格预设](references/prompts/style_presets.md) 和 [配色方案](references/prompts/color_palettes.md)。
- 需要参考完整样例时，读取 `examples/` 中与赛道最接近的案例。

## 视觉理解模式

触发条件：用户上传 1-10 张图片，并希望生成小红书标题、正文、标签、评论钩子或发布建议。

流程：

1. 读取 [动态澄清规则](references/dynamic_clarification.md)，判断内容目的、受众、语气、长度、商业意图和输出范围是否足够明确；不明确时先提问，确认后再生成。
2. 读取 [图片理解提示词](references/image_understanding_prompt.md)，逐张图片编号，提取可见事实、合理推断、不确定信息、时效关系、可写角度和风险点。
3. 读取 [主题确认与选题规则](references/topic_selection.md)，根据图片可见事实和合理推断生成 3-5 个主题选项，最后一个选项必须允许用户自定义输入主题；用户确认主题前不得继续生成最终笔记。
4. 用户确认主题后，按 [动态澄清规则](references/dynamic_clarification.md) 补问缺失的内容方向、字数范围和内容风格；已明确的信息不重复询问。
5. 基于已确认主题生成 3-5 个选题建议，并推荐一个最稳妥选题；用户选定选题或明确授权使用推荐选题后继续。
6. 获取当前公历日期、星期、月份；中文内容尽量确认农历日期、节气、法定节假日、平台节点、电商节点和近期热点。不能确认时只写“可借势方向”，不写成事实。
7. 判断适合赛道、目标人群、内容价值点和图片排序；图片不足、重复或缺关键细节时提出补拍建议。
8. 生成标题前联网搜索：按 [公式联网刷新规则](references/live_formula_refresh.md) 基于已确认主题和选定选题搜索实时新闻、节点、热梗、平台表达、热门标签和标题表达；联网不可用或无可靠结果时回退内置公式库。
9. 读取标题、正文、标签、评论钩子和风险资料，生成完整图文笔记。
10. 只基于可见信息写作；需要补充设定时用“可替换字段”呈现。
11. 不调用生图脚本，除非用户明确要求基于现有图片继续生成变体图。

## 文字规划模式

触发条件：用户提供主题、关键词、产品卖点、想法、草稿或目标人群，希望策划完整图文笔记。

流程：

1. 读取 [动态澄清规则](references/dynamic_clarification.md) 和 [主题确认与选题规则](references/topic_selection.md)，即使用户只提供文字主题或明确说“直接生成”，也先生成 3-5 个主题选项并要求用户确认；最后一个选项必须允许用户自定义输入主题。
2. 用户确认主题后，补问缺失的内容方向、字数范围和内容风格；已明确的信息不重复询问。
3. 提炼内容定位：赛道、受众、场景、核心利益点、笔记目标。
4. 基于已确认主题生成 3-5 个选题建议，并推荐一个最稳妥选题；用户选定选题或明确授权使用推荐选题后继续。
5. 生成标题前联网搜索：按 [公式联网刷新规则](references/live_formula_refresh.md) 基于已确认主题和选定选题搜索实时新闻、节点、热梗、平台表达、热门标签和标题表达；联网不可用或无可靠结果时回退内置公式库。
6. 建议图片张数；默认 5-7 张，教程型可 6-9 张，轻量种草可 3-5 张。
7. 为每张图输出可直接用于生图模型的 Prompt，包含主体、场景、构图、风格、配色、文字区域要求和负面约束。
8. 生成 3-5 个标题、正文、标签组合、评论钩子和发布建议。
9. 若用户要求直接生图，按“生图配置与调用”执行；若脚本、Python 环境或输出目录不可用，按 [脚本降级输出规则](references/fallback_outputs.md) 输出 ASCII 示例图和 Prompt，不中断正文、标题、标签和评论钩子交付。

## 发布后复盘模式

触发条件：用户提供已发布笔记内容、发布数据、截图、小红书单篇笔记链接或小红书主页链接，并希望复盘表现、诊断问题、打分、生成下一篇建议或本地 HTML 看板。

流程：

1. 读取 [发布后复盘规则](references/post_publish_review.md)，识别输入来源：手动数据、截图识别、单篇链接或主页链接。
2. 链接只抓取公开可见信息；不登录、不绕过验证码、不采集私信、订单、后台等敏感数据。抓取失败或字段不可见时，友好提示用户提供截图或文字信息。
3. 复盘时不强制执行新笔记的主题确认；若用户要求基于复盘结果生成下一篇完整笔记，再按“文字规划模式”进入主题确认、选题和成稿流程。
4. 把“小眼睛/观看/阅读/查看数”视为 `metrics.views`，把曝光视为 `metrics.exposure`，二者不能混用；链接解析不到 `views` 时，先追问用户补充小眼睛/观看数，用户明确回复“跳过”后才输出不完整流量诊断。
5. 基于用户提供或公开可见的数据做流量漏斗诊断：曝光（如有）→ 观看/小眼睛 → 点赞/收藏/评论/分享 → 关注/转化。缺失数据不猜测，标注为“数据不足”。
6. 复盘时联网搜索同类笔记/赛道的目标人群、搜索关键词、热门标签和常见标题表达，用于判断封面标题点击力、人群匹配和搜索流量潜力；联网不可用时明确说明。
7. 给出“流量诊断 + 行动建议”：数据结论、卡点判断、封面标题点击诊断、目标人群与关键词匹配、当前这篇补救动作、下一篇迭代实验。
8. 六维评分只作为附录参考保留：入口吸引力、标题搜索力、正文承接力、收藏价值、评论互动、转化/关注潜力。不要把总分或六维评分放在主结论。
9. 用户要求 HTML 看板时，将复盘结果整理为 JSON，并调用：`python3 scripts/generate_review_report.py --input <review.json> --output-dir outputs/reports`。
10. HTML 看板必须是离线单文件，默认内嵌用户图片，首屏展示数据结论、流量漏斗、封面标题点击诊断、目标人群与关键词匹配、当前补救和下一篇迭代；六维雷达图放入附录。
11. 若 HTML 报告脚本、Python 环境或输出目录不可用，按 [脚本降级输出规则](references/fallback_outputs.md) 输出 `## 发布后复盘报告（Markdown 降级版）`，用 Markdown 表格替代图表和 HTML 文件，并明确说明未生成真实 HTML。
12. 不承诺爆款或确定算法结论，只写“可能原因”“建议验证”“下一轮实验”。

## 生图配置与调用

真实配置文件路径为 `config/image_model.json`，该文件应被 `.gitignore` 忽略。仓库只提供模板 `config/image_model.example.json`。

用户可以通过两种方式配置：

- 手动复制模板并填写：`cp config/image_model.example.json config/image_model.json`
- 在对话中提供 `provider`、`base_url`、`api_key`、`model`、`size`、`quality`、`timeout_seconds`，由 Agent 在用户授权后写入 `config/image_model.json`

调用规则：

- API Key 只写入本地配置或环境变量，不在最终回答中完整展示。
- 环境变量优先级高于配置文件：`XHS_IMAGE_PROVIDER`、`XHS_IMAGE_BASE_URL`、`XHS_IMAGE_API_KEY`、`XHS_IMAGE_MODEL`、`XHS_IMAGE_SIZE`、`XHS_IMAGE_QUALITY`、`XHS_IMAGE_TIMEOUT_SECONDS`。
- 调用脚本前检查 `scripts/generate_images.py`、`python3`、prompts 文件和输出目录是否可用；任一不可用时，不要声称已生成真实图片，改用 ASCII 示例图降级输出。
- 生图脚本：`python3 scripts/generate_images.py --prompts <prompts.json> --output-dir outputs/images`
- 试运行请求体：`python3 scripts/generate_images.py --prompts <prompts.json> --dry-run`

## 脚本可用性与降级输出

当用户环境中的 `scripts/` 目录丢失、目标 `.py` 文件丢失、`python3` 不存在、脚本执行失败、权限不足或输出目录不可写时，按 [脚本降级输出规则](references/fallback_outputs.md) 继续交付。

- 生图不可用：输出 ASCII 示例文字图片、图片用途说明和可复制 Prompt；明确写“这是示意图，不是真实生成图片”。
- HTML 报告不可用：输出 `## 发布后复盘报告（Markdown 降级版）`，用数据结论、流量漏斗表、封面标题点击诊断、目标人群与关键词匹配、当前补救动作、下一篇实验卡和附录参考评分替代 HTML 看板。
- 降级输出仍必须完成标题、正文、标签、评论钩子、复盘结论或下一篇建议，不把脚本不可用当作整体任务失败。

## 输出模板

每次交付图文笔记时必须使用“两段式”结构：

1. 先输出 `## 可复制发布区`，并用一个整体 Markdown 代码块包住可直接复制发布的内容。
2. `可复制发布区` 内必须同时包含图片、标题、正文、标签、评论钩子；不要把这些内容拆到代码块外。
3. 代码块之后再输出 `## 分析说明`，用于解释内容定位、图片分析、创作逻辑、风险检查和发布建议。
4. 如果用户只要标题和文案，仍保留同样结构；图片部分写“使用用户上传图片作为封面/配图”或给出图片排序建议。

````markdown
## 可复制发布区

```markdown
# 图片
1. 图1：[封面/配图作用]
   - 使用：[用户上传图片 / 生成图片文件路径 / 建议画面]
   - 画面要点：[主体、场景、构图、情绪]
   - Prompt：[仅在需要生图或用户要求 Prompt 时填写]

# 标题
推荐标题：[最推荐发布标题]

备选标题：
1. [标题1]
2. [标题2]
3. [标题3]

# 正文
[可直接发布的小红书正文]

# 标签
#标签1 #标签2 #标签3 #标签4 #标签5 #标签6 #标签7 #标签8

# 评论钩子
置顶评论：[置顶评论内容]
引导问题：[评论区互动问题]
```

## 分析说明

### 内容定位
- 赛道：
- 目标人群：
- 核心卖点：
- 笔记角度：

### 智能选题
- 已确认主题：
- 选题候选：
- 推荐选题：
- 选择理由：

### 图片分析
- 图片排序：
- 画面亮点：
- 可见事实：
- 合理推断：
- 不确定信息：
- 时效关系：
- 补图建议：

### 趋势刷新
- 是否联网：
- 查询方向：
- 采用结果：
- 回退说明：

### 创作逻辑
- 标题逻辑：
- 正文结构：
- 标签策略：
- 评论钩子逻辑：

### 风险检查
- 已规避：
- 建议替换：
- AI 标注：

### 发布建议
- 封面：
- 发布时间：
- 互动动作：
````

## 质量标准

- 标题：3-5 个备选，兼顾点击欲望、信息密度和合规表达。
- 可复制发布区：图片、标题、正文、标签、评论钩子必须在同一个 Markdown 代码块内，方便用户一次复制。
- 正文：开头 3 秒内给出钩子；中段具体；结尾引导收藏、评论或行动。
- 标签：8-12 个为默认范围，包含赛道词、场景词、痛点词、长尾词和品牌/地点词。
- 热梗热词：每篇最多使用 1-2 个明显热梗，优先自然表达；不确定是否仍流行时改用稳妥表达。
- 评论钩子：至少 2 条，避免诱导虚假互动或夸大承诺。
- 风险检查：覆盖违禁词、功效承诺、医疗金融风险、广告法绝对化用语和 AI 生成标注。
